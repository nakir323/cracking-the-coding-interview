const SLL = require("../../helper/singly-linked-list");

/**
 * 1 -> 2 -> 3 -> 4 -> 5 -> 3（1つ目の3と同じオブジェクト）
 * となっている場合には、3
 */
function findCircular(head) {
  return null;
}

module.exports = {
  findCircular,
};
